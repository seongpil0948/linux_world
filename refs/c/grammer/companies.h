#include <stdio.h>

namespace intellisys {
    void Display() {
        printf("Display");
    }; // 함수의 원형
    int count;      // 변수의 선언
    char name[20] = "Intellisys";
}

namespace lhs {
    double display; // 변수의 선언
    int count;      // 변수의 선언
    char name[20] = "Lotte Home Shopping";
}